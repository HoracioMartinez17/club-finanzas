# Base de datos
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/club_finanzas"

# Authentication
JWT_SECRET="crear-una-clave-segura-aleatoria-aqui"
AUTH_SECRET="crear-una-clave-segura-aleatoria-aqui"

# Test Credentials (CHANGE IMMEDIATELY IN PRODUCTION)
# Leave empty to use defaults in seed.ts, or set custom values
TEST_ADMIN_PASSWORD="***REMOVED***"
TEST_TESORERO_PASSWORD="***REMOVED***"

# Super Admin Credentials (REQUIRED in production)
# If not set, will use defaults from create-super-admin.ts (development only)
SUPERADMIN_EMAIL="superadmin@clubfinanzas.com"
SUPERADMIN_PASSWORD="create-a-secure-random-password-here"

# Test Club Admin Credentials (used by create-test-club.ts script)
# Usage: ts-node prisma/create-test-club.ts --password='YourSecurePassword'
TEST_CLUB_ADMIN_PASSWORD="create-a-secure-random-password-here"

# Prisma
DATABASE_URL_PRISMA="prisma+postgres://accelerate.prisma-data.net/?api_key=tu_api_key"

# Vercel (optional)
VERCEL_OIDC_TOKEN="opcional-para-deployment"

# Environment
NODE_ENV="development"
